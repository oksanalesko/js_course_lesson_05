"use strict"
// Введення даних, позначення величин



// Обчислення результатів



// Виведення результатів

for (let monthNum = 3; monthNum <= 8; monthNum++) {
   if (monthNum >= 3 && monthNum <= 5)
      document.write(`<p>Весняний місяць № ${monthNum}</p><br>`)
   else document.write(`<p>Літній місяць № ${monthNum}</p><br>`)
}